# 内容整理规范

本文件适用于 `docs/books/<topic>/` 下的章节页面。专题首页 `index.md`、站点首页和配置文件不要求套用章节结构。

整理后的章节应围绕核心问题展开，兼顾理解、动手验证和复习。每章必须包含“本章要点”“练习”和“参考资料”，通常在练习之后设置“面试题”；符合下文适用条件时可以省略面试题。正文结构可以根据主题调整，参考答案统一折叠展示。

## 处理提供的材料

用户提供的材料可以是原文、笔记或验证记录，也可以组合使用：

- **原文**：保留书名、章节、页码或来源链接等出处信息。
- **笔记**：提炼用户的理解、类比、代码示例和疑问。
- **代码验证记录**：可以作为练习答案的一部分，记录必要的运行环境、命令、实际结果和分析。

材料不完整时，使用已有内容继续整理，并明确标注缺失信息、待核验结论或仍需回答的问题。

## 章节结构

章节统一采用以下顺序：

1. 页面元信息与一级标题
2. 本章要点
3. 正文
4. 练习
5. 面试题（适用时）
6. 参考资料

只有正文二级小节按书籍的章、节连续编号；正文中的三级标题按所属二级小节连续编号为 `X.Y.Z`，四级及更深标题不编号。练习题和面试题分别编号为“练习 N”“面试题 N”，不使用正文的 `X.Y.Z` 编号。“本章要点”“练习”“面试题”和“参考资料”均不参与二级小节编号。统一使用以下骨架；省略面试题时同时省略对应标题：

```markdown
---
title: 第 X 章 章节名称
description: 用一句话概括本章内容。
---

# 第 X 章 章节名称

## 本章要点

本章范围、主线和学习目标。

> 核对日期：YYYY 年 M 月 D 日；核对环境为软件版本、操作系统/架构。

## X.1 正文小节 {#chX-1}

正文内容。

### X.1.1 正文三级小节

## 练习 {#chX-exercises}

用简短导语说明本节覆盖范围、作答方式及代码上下文。

### 练习 1：题目名称

## 面试题 {#chX-interview}

用简短导语说明本节自测重点及必要的代码约定。

### 面试题 1：问题名称

## 参考资料

- [资料名称](https://example.com/)
```

## 固定部分规范

### 题目设计总则

练习题和面试题共同承担本章自测功能，应覆盖本章的核心知识、重难点和高频易错点。整理前先根据“本章要点”和正文列出知识点，再建立知识点与题目的对应关系；不要求每个细节都单独出题，优先覆盖会影响后续学习或实际编码的内容。

每道题必须有独立且明确的主要考察意图。练习与面试题应一起去重：相同知识点只有在考察层次或边界明显不同（例如实现与规则判断、编译期与运行期）时才再次出现，否则应合并或删除；不得只替换数值、变量名或表述来制造重复题。题目数量以覆盖必要知识点为限，优先合并关联的易错点，删去机械记忆、低区分度和对自测结论没有帮助的题目。知识点较少时可以少出题，不为凑数增加无关考点。

题目中的代码应短小、完整或提供足够的上下文，只保留体现考察意图所必需的声明和语句；删除与考点无关的初始化、输出和业务背景。代码、命令和结论必须经过语法与版本核对，涉及版本、平台或实现差异时明确标注；会影响作答结论的条件须在题目中给出。完成整组题目后，读者应能判断自己是否掌握本章主线，并定位尚未理解的知识点。

练习和面试题的二级标题后各用一小段导语概括覆盖范围，并提示先独立作答、再展开答案；写出题数时须与实际题目一致，不逐题重复标题。有代码片段时，导语应说明所在上下文和省略的导入约定，单题的特殊条件在题干补充；两部分约定相同时，后者可简短引用前者。导语不得提前透露答案。

### 本章要点

“本章要点”必须紧跟一级标题。用一至两段说明本章解决的问题、内容主线和读完后应掌握的能力，不要重复目录，也不要提前展开正文细节。

要点之后使用引用块标明核对日期和核对环境。日期为资料最后核验的时间；环境至少包含主要软件或语言版本及操作系统/架构。若本章用到有最低版本要求的语言特性、命令或库，在引用块第二段集中说明最早支持版本；没有明确的兼容性要求时不添加第二段。例如：

```markdown
> 核对日期：2026 年 9 月 11 日；核对环境为 Go 1.27.1、Windows/AMD64。
>
> `clear`、标准库 `slices` 和 `maps` 需要 Go 1.21 或更新版本。
```

### 练习

练习用于检查概念理解、规则运用和代码阅读能力，不以运行程序为完成前提。练习部分使用不编号的二级标题 `## 练习 {#chX-exercises}`；每题从 1 开始连续编号，标题写成 `### 练习 1：题目名称`。

题目应写清已知条件、问题和完成要求。参考答案紧随题目，说明推理过程、关键依据和结论；存在多种合理答案时，明确判断标准并给出代表性思路。

练习可以要求编写、修改或阅读代码，运行可作为完成后的可选核验；需要记录运行结果时，将结果放入参考答案。答案应重视完整推理、实现思路和可迁移的规则。

练习题优先安排为少量、递进的任务：先检查关键规则，再要求综合运用或修改代码。可以同时覆盖一个主要知识点及其直接易错点，但不要把多个无关主题拼在一道题里。

### 面试题

面试题用于通过直接问答检查关键规则和易错点，不以运行程序为前提。选择能区分理解程度的场景，使用简短代码或直接问题，要求回答“能否编译”“输出什么”“为什么”或“如何修复”等。使用不编号的二级标题 `## 面试题 {#chX-interview}`；每题从 1 开始连续编号，标题写成 `### 面试题 1：问题名称`。

参考答案先给结论，再给必要的关键依据和版本、平台边界，不展开练习式的完整实现过程。

若本章内容主要依赖完整任务或操作流程，无法形成有独立价值的简短问答，且关键知识已由练习覆盖，可以省略面试题部分。在练习导语中用一句话说明原因，不保留空标题，也不从其他章节借题凑数。

### 参考资料

“参考资料”位于章节末尾，使用不编号的二级标题 `## 参考资料`。只列为本章事实核对、机制解释或延伸阅读实际使用的外部资料，优先选择语言规范、官方文档、标准文档、源码和发布说明等一手来源。链接文字应描述具体内容，并尽量指向相关章节，而不是笼统的站点首页。

参考资料不列出当前文档所依据、整理或改写的原书籍；原书信息应在专题首页或统一书目中说明。正文若直接引用任何资料，仍须在引用处标明出处。不要列出正文没有使用的链接，也不要重复列出内容相同的入口。

## 正文内容组织

正文根据主题需要组织以下内容，不要为了形式创建空小节：

- **概念与原理**：交代必要的前置知识，解释关键术语、规则及适用边界。
- **推导、流程或对比**：梳理因果关系、执行过程、设计取舍或不同方案的差异。
- **代码与示例**：用简洁、可运行的例子支撑解释，说明关键行为和常见误区。代码片段使用包限定符但未展示完整 `import` 块时，应在首次出现处说明对应导入路径，尤其要区分名称相近或不同版本的包。

网络章节可以围绕一次请求的时序组织正文，再通过练习分析请求过程、通过运行记录核对关键行为；数据库章节可以围绕执行计划展开，再通过查询练习和对照结果检查理解。

## 参考答案格式

练习和面试题的答案均须默认折叠。题目、已知条件、待分析代码和完成要求放在折叠区域外；解答代码、预期输出、推理过程和结论放在对应的折叠区域内，避免提前揭示答案。开放性题目可以提供参考思路，结果依赖环境时说明可能的差异。

统一使用以下 Markdown 写法，不添加 `open` 属性；每道练习或面试题的答案紧随其后：

```markdown
### 练习 1：题目名称

在这里写明题目、已知条件和完成要求。

<details>
<summary>查看参考答案</summary>

在这里给出解答、关键依据，以及必要的代码和输出。

</details>
```

面试题使用相同的折叠结构，标题改为“面试题 1：问题名称”，摘要写为“查看参考答案”。

`<summary>` 后和 `</details>` 前保留空行，确保其中的段落、列表和代码块正常渲染。

## 校对与润色

整理时保留原意和学习重点，修正事实错误、术语误用、逻辑跳跃和重复表达，补齐理解所需的上下文。表述应直接、准确，先解释规则，再用示例说明，避免只给结论而缺少依据。

当前站点及其章节统一称为“本指南”，不要用“本书”指代；引用作为整理依据的书籍时使用“原书”或完整书名。确定专题中的主要术语后，页面元信息、一级标题、正文、练习、面试题、目录和侧边栏必须保持一致；需要交代其他常见译名时，只在术语首次出现处括注，后文继续使用主要术语。

明确区分原文引用、官方资料、个人理解和运行观察。直接引用保留原文并注明出处；原文有误或已过时时，在引用之外补充更正及依据。涉及版本差异或实现细节时，标明适用版本与资料日期，避免把某次运行结果写成普遍保证。

核对代码与命令的语法和运行条件，保证结果说明与实际验证一致。未经验证的推测必须明确标注，不能写成已确认的事实。

## 文件约定

章节文件放在 `docs/books/<topic>/` 下，图片放在该专题的 `assets/` 目录，并使用相对路径引用。文件名使用小写英文、数字与短横线；按书籍顺序整理时沿用 `chapter-03.md` 这样的编号，独立主题使用 `slice-growth.md` 这样的描述性名称。

页面顶部保留 `title` 和 `description` 元信息，正文只使用一个一级标题，并按层级组织小节。新增章节时同步更新对应的目录或侧边栏，确保页面可以被找到。

### 自定义锚点与交叉引用

章节的每个带编号二级正文标题都在标题末尾添加 `{#chX-Y}`，其中 `X` 是章号、`Y` 是节号；练习和面试题分别使用 `{#chX-exercises}`、`{#chX-interview}`。需要稳定深层链接的三级或四级标题也在标题末尾使用 `{#...}`，ID 应以所属章节的 `chX-Y-` 开头，后接小节号或简短的英文主题名，例如 `{#ch4-4-4}`、`{#ch2-6-var}`。ID 由小写英文、数字和短横线组成，并在当前页面内保持唯一。标题的规范锚点统一使用 `{#id}`，不要在标题前放置独立的 `<a id="..."></a>`；只有兼容旧链接或锚定非标题位置时才使用 `<a>`，且它不能替代标题自身的规范锚点。

引用已经存在的章或节时使用 Markdown 链接，不保留“详见第 X 章”“见 X.Y 节”之类的纯文本引用。同一页面使用 `#anchor`，其他章节使用 `./chapter-04#ch4-2` 这样的相对链接，并优先指向上述稳定锚点。目标章节尚未创建时不要添加死链，改用不带编号的说明，例如“将在后续的接口章节展开”。修改章节名称或主要术语时，同时检查页面元信息、一级标题、站点目录、侧边栏和其他章节中的链接文字。

## Git 提交规范

提交消息的第一行用一句简洁的话概括本次提交的整体内容；后续用无序列表逐项说明所有变更内容，覆盖新增、修改、删除和重命名及其目的，但不要求列出具体文件名或路径。提交消息应准确反映实际差异，不使用含糊的“更新内容”或遗漏部分修改。例如：

```text
统一章节格式并移除站点指南页面

- 迁移章节整理规范，统一内容整理要求
- 删除不再使用的站点指南页面
- 清理首页和站点配置中的对应入口
```

执行 `git commit` 前，必须先向用户展示拟使用的提交标题、完整的修改列表和待提交文件清单，并等待用户明确确认。确认前不得执行 `git commit`；用户未确认时，只能继续检查或修改内容。获得确认后，再按展示的内容完成暂存并执行提交；若实际差异发生变化，应重新展示并再次等待确认。
